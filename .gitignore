# ------------------------------------
# 1. Build and Compilation Artifacts
# ------------------------------------
# Exclude the executable for the current task (e.g., spmxv.exe)
*.exe
# Exclude object files (.o) and dependency files (.d) created during compilation
# The Makefile cleans these up, but it's good practice to ignore them.
*.o
*.d
# Exclude the 'clean' target artifacts specified in the Makefile
# This ensures a clean source state is always available.
${RM}
renv

# ------------------------------------
# 2. Benchmark and Competition Results
# ------------------------------------
# Exclude the main benchmarking results directory.
# These files are generated by collect-benchmark.sh and parse-benchmark.sh.
/benchmarking/
# Exclude the competition results directory.
/competition/

# ------------------------------------
# 3. Virtual Environments and R/renv cache
# ------------------------------------
# Exclude Python Virtual Environment directories (as used in visualization setup)
venv/
# Exclude R/renv temporary directories and cached files
/renv/library/
/renv/cache/
# renv itself has some files to ignore already (renv/.gitignore), but these are common
# Exclude local R files generated by R/renv
.RData
.Rhistory

# ------------------------------------
# 4. Matrix files, slurm output files etc.
# ------------------------------------
# Exclude job output files generated during batch/interactive runs
*.txt